# MongoDB connection string (never commit the real one)
MONGODB_URI=mongodb+srv://user:password@cluster0.mongodb.net/eicher_calculation?retryWrites=true&w=majority

# Server port
PORT=4000

# Comma separated list of allowed frontend origins ("*" allows all)
CORS_ORIGIN=*
